<%= form_for(@route_order) do |f| %>
  <% if @route_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@route_order.errors.count, "error") %> prohibited this route_order from being saved:</h2>

      <ul>
      <% @route_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
